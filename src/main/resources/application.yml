spring:
  application:
    name: 'welvia_incomes_service'
  profiles:
    active: dev
  datasource:
#    url: "jdbc:postgresql://127.0.0.1:5432/financial-targets-db"
    url: "jdbc:postgresql://host.docker.internal:5432/financial-targets-db"
    username: postgres
    password: G~w<Yq=&fU;93kNLZ
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    baseline-on-migrate: true
    baseline-version: 1
server:
  port: 8080
  servlet:
    contextPath: /api
logging:
  level:
    root: WARN
    com.welvia: DEBUG  